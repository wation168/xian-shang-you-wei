# 專案結構盤點｜雜湊補充報告

> 本文件為對 `project_structure_inventory.md`（原報告，16,698行）的**唯讀補充**，不修改、不重寫原報告。原報告全部照舊保留。本文件經過兩輪修訂：第一輪處理(1) SHA-256比對範圍不完整、(2) 第五節`scan_result.html`誤判；第二輪修訂（本次）直接核對`enrich_lottery_v3.py`原始碼確認import依賴、將第五節其餘7項改為六類細緻分類並修正「斷裂／矛盾引用數」、並將SHA-256相同組數重新分類避免被誤讀為可刪除清單。過程中未移動、刪除、重新命名或修改任何既有檔案，未重新計算SHA-256（沿用第一輪雜湊結果）。

**補充盤點時間**：2026-07-27（原報告同日追加，本次為同日第二輪修訂）

> ⚠️ **警語**：本文件第一節「SHA-256內容相同組數」只代表這些檔案的**雜湊值一致**，**不代表可以刪除**。是否可清理，需另外參考原報告第三節「檔案分類建議」（不能亂動／安全可搬移候選／不確定）判斷，本文件不單獨構成清理依據。

---

## 一、SHA-256 完整補跑結果

### 1.1 補跑範圍與結果

原報告第一節統計「共21,072個檔案」，但第四節SHA-256比對只涵蓋16,137個檔案，缺漏以下6個目錄、共**4,935個檔案**：

| 目錄 | 檔案數 |
| -- | -: |
| `backend/frontend/tools/` | 3,245 |
| `backend/frontend/glossary/` | 218 |
| `backend/frontend/patterns/` | 764 |
| `backend/frontend/comparisons/` | 310 |
| `backend/frontend/blog/` | 120 |
| `backend/frontend/lottery/` | 278 |
| **合計** | **4,935** |

本次已對這4,935個檔案**全部**重新讀取並計算SHA-256，**無任何檔案讀取失敗或權限錯誤**（0 errors）。合併回原有16,137個已雜湊的檔案後：

- **合併後總檔案數：21,072**（與原報告第一節「檔案總數21,072」完全吻合，確認無遺漏、無重複計算）
- **全部21,072個檔案的路徑皆唯一**（無重複路徑，排除計算錯誤的可能性）

✅ **本次SHA-256比對已涵蓋全部21,072個檔案，非抽樣、非部分。**

### 1.2 全專案完整重複比對：總體結果

以合併後的21,072個檔案雜湊值重新做全專案分組。**本節統計名稱由原本的「完全相同重複檔案組數」更正為「SHA-256內容相同組數」**，以避免被誤讀成「這些都是可以刪除的重複檔案」——雜湊相同只代表位元組內容一致，實際能否清理仍須參考原報告第三節分類。

| 項目 | 原報告數字（僅16,137檔） | 補跑後數字（全21,072檔） |
| -- | -: | -: |
| **SHA-256內容相同組數**（原稱「完全相同重複檔案組數」） | 42 | **1,193** |

**差異來源說明**（1,193組是如何從42組變化而來，逐一核對，無不明差異）：

- 原本42組中，有 **31組** 因為新雜湊的檔案剛好與該組內容相同而「多了一名成員」（不是新增組，是原組擴大）：
  - 30組是彩票JSON資料：`scripts/lottery_data/{results,stats}/*.json` 與 `backend/frontend/lottery_backup_20260727_004456/{data,stats}/*.json` 這兩兩相同的組，現在確認 `backend/frontend/lottery/{data,stats}/*.json`（目前實際服務中的版本）也是同一份內容——代表**這30個彩票資料檔案，三個位置的內容完全一致**。
  - 1組是 `tools.css`：原本`tools_backup/tools.css`與`backend/frontend/tools_backup_charts/tools.css`相同，現在確認`backend/frontend/tools/tools.css`（目前實際服務版本）也完全相同——代表這份共用CSS三個世代都沒改過。
- 原本42組中，其餘 **11組** 維持不變（不涉及本次新雜湊的6個目錄），清單如下：
  - `git`／`backend/cd`／`backend/dir`／2份`.gitkeep`／2份`stock_picker`下的`.gitkeep`（皆為0 bytes空檔案）
  - `tools_index_backup.html`＝`tools_backup/index.html`
  - `backend/frontend/search-index.json`＝`backend/frontend/common/search-index.json`
  - `backend/frontend/common/softglow-common.css`＝`knowledge-engine/softglow-common.css`
  - `backend/stock_picker/crawler.py`＝`stock_picker/crawler.py`
  - `backend/stock_picker/finmind_filter.py`＝`stock_picker/finmind_filter.py`
  - `backend/stock_picker/main_picker.py`＝`stock_picker/main_picker.py`
  - `backend/stock_picker/README.md`＝`stock_picker/README.md`
  - `backend/stock_picker/scheduler.py`＝`stock_picker/scheduler.py`
  - `knowledge-engine/lottery_data/lottery_games.json`＝`scripts/lottery_data/lottery_games.json`
  - `knowledge-engine/lottery_data/seo_pages.json`＝`scripts/lottery_data/seo_pages.json`
- 新增 **1,151組**，全部因為這次新雜湊的6個目錄與既有內容比對後產生，明細見下方1.3節。

**驗算**：31（擴大）+ 11（不變）+ 1,151（新增）= 1,193 ✅ 與程式計算結果一致，無不明差異。

#### 1,193組「SHA-256內容相同」的性質分類

**再次提醒：以下分類只是說明「為什麼內容相同」，不是清理建議。任何一類都不應直接視為可刪除，清理與否請對照原報告第三節。**

已對全部1,193組逐組依「涉及哪些目錄」重新程式化分類，結果如下：

| 分類 | 組數 | 說明 | 是否建議視為冗餘 |
| -- | -: | -- | -- |
| **A．預期同步副本／部署鏡像** | **971** | 幾乎全部（970組）是 `backend/frontend/patterns/` ↔ `knowledge-engine/output/patterns/`（660組）與 `backend/frontend/comparisons/` ↔ `knowledge-engine/output/comparisons/`（310組）——這是生成流程刻意保留的「產出來源」與「服務目的地」，兩邊內容完全同步、非過時副本。另有1組是`backend/frontend/patterns/`資料夾內部三個語言清單檔（`pattern_en_list.txt`／`pattern_ja_list.txt`／`pattern_ko_list.txt`）恰好內容相同，屬語言巧合非備份關係 | **明確不建議視為冗餘**——`knowledge-engine/output/{patterns,comparisons}/`很可能是重新生成`backend/frontend/`對應內容時的必要中繼產物，刪除可能導致往後無法重新生成或比對 |
| **B．正式資料與備份版本一致** | **211** | 細分：彩票開獎資料JSON三處一致（30組：`scripts/lottery_data/` = `backend/frontend/lottery_backup_20260727_004456/` = `backend/frontend/lottery/{data,stats}/`）；`lottery_backup_20260727_004456/`中156個HTML頁面恰好與目前服務版本相同（278個中的其餘92個不同，見1.4節）；`blog_backup_svgs/`中10個語言首頁與目前服務版本相同（120個中其餘110個不同）；tools家族15組（`tools.css`、`check_tools.py`、`fix_tools_batch.py`、`fix_tools_batch_v2.py`、`tools-chart.js`及10個語言`index.html`，服務版與`tools_backup`/`tools_backup_charts`部分相同） | 這是「備份版本剛好還沒被後續更新覆蓋到」的部分，**不是要不要刪除的問題，而是這些備份目錄本身是否還要保留**（見原報告第三節3.3「不確定」分類），單一組本身不建議直接視為冗餘 |
| **C．真正疑似冗餘、可考慮清理的重複** | **11** | 即原報告已列出、且本次補跑後仍維持不變的11組：`git`／`backend/cd`／`backend/dir`／4份`.gitkeep`（空檔案巧合）、`tools_index_backup.html`＝`tools_backup/index.html`、`backend/frontend/search-index.json`＝`common/search-index.json`、`backend/frontend/common/softglow-common.css`＝`knowledge-engine/softglow-common.css`、根目錄`stock_picker/`與`backend/stock_picker/`下5個同名模組（`crawler.py`／`finmind_filter.py`／`main_picker.py`／`README.md`／`scheduler.py`） | 這批是**最接近「單純多餘副本」的候選**，但仍請對照原報告第三節3.2「安全可搬移候選」逐項確認後再處理，本文件不代替該節做清理決策 |
| **合計** | **1,193** | 971 + 211 + 11 = 1,193 ✅ | — |

### 1.3 四個工具頁目錄比對（`backend/frontend/tools/` vs `tools_backup/` vs `output/tools/` vs `backend/frontend/tools_backup_charts/`）

| 目錄 | 檔案數 |
| -- | -: |
| `backend/frontend/tools/`（目前實際服務版本） | 3,245 |
| `tools_backup/` | 3,234 |
| `output/tools/` | 3,229 |
| `backend/frontend/tools_backup_charts/` | 3,245 |

**四目錄共有同名檔案（3,229個檔名在四邊都存在）：全部逐一比對，沒有任何一個檔名的內容在四邊完全一致（0/3,229）**——代表這是四個不同世代的生成結果，不是單純重複備份。

**兩兩比對明細**：

| 比對組合 | 共同檔名數 | 內容相同 | 內容不同 |
| -- | -: | -: | -: |
| `backend/frontend/tools/` vs `tools_backup/` | 3,233 | **1**（`tools.css`） | 3,232 |
| `backend/frontend/tools/` vs `output/tools/` | 3,229 | **0** | 3,229 |
| `backend/frontend/tools/` vs `tools_backup_charts/` | 3,245 | **15** | 3,230 |
| `tools_backup/` vs `output/tools/` | 3,229 | **0** | 3,229 |
| `tools_backup/` vs `tools_backup_charts/` | 3,233 | **1**（`tools.css`，與上方同一份） | 3,232 |
| `output/tools/` vs `tools_backup_charts/` | 3,229 | **0** | 3,229 |

`backend/frontend/tools/` 與 `backend/frontend/tools_backup_charts/` 相同的15個檔案明細（全部是列表頁或共用資源，**沒有任何一個是個別計算機工具頁**）：
`check_tools.py`、`fix_tools_batch.py`、`fix_tools_batch_v2.py`、`index.html`、`tools-chart.js`、`tools.css`、`de/index.html`、`en/index.html`、`es/index.html`、`fr/index.html`、`id/index.html`、`ja/index.html`、`ko/index.html`、`pt/index.html`、`zh-CN/index.html`

**結論**：目前實際服務中的 `backend/frontend/tools/` 有 3,245 個檔案，其中僅15個（0.5%）能在任一備份/生成目錄中找到內容完全相同的對應版本，其餘 **3,230個個別計算機工具頁全部與所有備份版本內容不同**——代表這些工具頁後續都經過修改，三個備份目錄都是不同時間點的舊快照，彼此之間也互不相同。

**額外發現**：`backend/frontend/tools_backup_charts/` 內含 `check_tools.py`、`fix_tools_batch.py`、`fix_tools_batch_v2.py` 三支Python腳本（與`backend/frontend/tools/`下同名腳本內容相同）。原報告第2.5節僅提及`fix_tools_batch.py`一支，本次補充確認**還有`check_tools.py`與`fix_tools_batch_v2.py`兩支同樣放在服務目錄裡的維護腳本**；`backend/.zeaburignore`只排除了`fix_tools_batch.py`這個確切檔名，**未排除`check_tools.py`與`fix_tools_batch_v2.py`**，這兩支可能被誤打包進Zeabur部署。

### 1.4 正式版本 vs 對應備份／舊版本（blog／lottery／glossary／patterns／comparisons）

| 分類 | 正式版本目錄 | 檔案數 | 對應備份／舊版本目錄 | 檔案數 | 共同檔名 | 內容相同 | 內容不同 |
| -- | -- | -: | -- | -: | -: | -: | -: |
| glossary | `backend/frontend/glossary/` | 218 | `backend/output/glossary/` | 436 | 218 | **0** | 218 |
| patterns | `backend/frontend/patterns/` | 764 | `knowledge-engine/output/patterns/` | 660 | 660 | **660** | 0 |
| comparisons | `backend/frontend/comparisons/` | 310 | `knowledge-engine/content/comparisons/`（原始素材，檔名格式不同） | 310 | **0**（檔名格式完全不同，無法對應） | - | - |
| comparisons | `backend/frontend/comparisons/` | 310 | `knowledge-engine/output/comparisons/` | 310 | 310 | **310** | 0 |
| blog | `backend/frontend/blog/` | 120 | `backend/frontend/blog_backup_svgs/` | 120 | 120 | **10** | 110 |
| lottery | `backend/frontend/lottery/` | 278 | `backend/frontend/lottery_backup_20260727_004456/` | 278 | 278 | **186** | 92 |

**逐項說明**：

- **glossary**：`backend/output/glossary/` 與目前服務版本**218個同名檔案全部內容不同**，且`backend/output/glossary/`還多出218個服務版本沒有的檔案（主要是`.cache/`下的92個JSON及其餘語言頁面數量差異）。兩者是完全不同世代的生成結果，並非簡單的暫存複本。
- **patterns**：`knowledge-engine/output/patterns/`的660個檔案**全部**與目前服務版本內容一致，服務版本另外多出104個該處沒有的檔案。代表`knowledge-engine/output/patterns/`是與目前線上內容完全同步的生成來源（尚未過時）。
- **comparisons**：`knowledge-engine/content/comparisons/`是原始文字素材（檔名格式與服務版本完全不同，如`*_raw.txt`類型，無法逐檔對應比較）；`knowledge-engine/output/comparisons/`的310個檔案則**全部**與目前服務版本內容一致，同樣是完全同步、未過時的生成來源。
- **blog**：`blog_backup_svgs/`的120個檔案中，只有10個（各語言的`index.html`列表頁）與目前服務版本相同，**其餘110篇個別部落格文章內容都不同**——確認`blog_backup_svgs/`是加入SVG圖示前的舊版內容快照，該120篇文章的實際內容已經被後續更新取代。
- **lottery**：`lottery_backup_20260727_004456/`的278個檔案中，186個與目前服務版本相同，**92個不同**。經檢查，92個差異全部是HTML頁面（`index.html`、`daily-cash-history.html`、`taiwan-lotto-statistics.html`等history/statistics/結果頁，按語言分布：en 32、de/es/pt各10、根層8、fr/ja/ko各6、id/zh-CN各2），**不是**開獎資料JSON（JSON已在1.2節確認三處一致）。代表這個帶時間戳記的資料夾，其HTML樣板內容是稍舊的版本（缺少後續的頁面更新），但其資料檔恰好與目前資料一致（可能是因為備份建立後尚未有新一期開獎需要更新）。

### 1.5 修正後的統計數字（取代原報告第四節/第六節對應數字）

| 項目 | 原報告數字 | **修正後數字** |
| -- | -: | -: |
| **SHA-256內容相同組數**（原稱「完全相同重複檔案組數」，更名理由見文件開頭警語） | 42 | **1,193**（其中31組是原組擴大為三方相同，11組維持不變，1,151組為新發現；性質分類為A.預期同步副本/部署鏡像971組、B.正式資料與備份版本一致211組、C.真正疑似冗餘可考慮清理11組，見1.2節） |
| 同名但內容不同的檔案組數 | 13（僅限原本已雜湊範圍） | 原13組**維持不變**（未受本次補充影響，含`generator.py`、11對`frontend`/`backend/frontend`同名頁面、`.gitignore`/`gitignore`），**另外新增**下列因本次補跑而發現的「正式服務版本 vs 備份/生成版本」同名不同內容檔案，**不併入13組計算，另立統計**（因比對基準不同，避免混淆）： |
| 　－ tools家族同名不同內容 | 未比對 | **3,230**（`backend/frontend/tools/`中3,230個檔案，在`tools_backup/`／`output/tools/`／`tools_backup_charts/`三處均找不到內容相同的對應版本） |
| 　－ glossary同名不同內容 | 未比對 | **218**（全部218個服務版本檔案與`backend/output/glossary/`同名版本都不同） |
| 　－ blog同名不同內容 | 未比對 | **110**（`blog_backup_svgs/`中110篇文章與服務版本不同） |
| 　－ lottery同名不同內容 | 未比對 | **92**（`lottery_backup_20260727_004456/`中92個頁面與服務版本不同） |
| 　－ patterns同名不同內容 | 未比對 | **0**（660個共同檔名全部相同，完全同步） |
| 　－ comparisons同名不同內容 | 未比對 | **0**（310個共同檔名全部相同，完全同步） |

**若要一個總數**：原13組 + 新發現3,230+218+110+92=3,650個「正式版本與備份版本同名不同內容」的檔案實例 = **合計3,663個檔案層級的「同名不同內容」實例**（注意：這是檔案數而非「組數」，因為tools家族每個檔案可能同時與多個備份目錄比較；若需要更精細的組別劃分請參照1.3、1.4節的兩兩比對表）。

**權限/讀取錯誤**：本次補跑4,935個檔案與後續全專案21,072個檔案的雜湊計算，**全部成功，0個檔案發生權限錯誤或讀取失敗**。

---

## 二、`scan_result.html` 誤判：正式撤回

### 2.1 撤回聲明

原報告第五節第1項結論：

> `backend/main.py:7561-7564` 讀取 `scan_result.html` 時沒有存在性檢查，可能導致未處理的500錯誤

**本項結論錯誤，正式撤回。**

### 2.2 重新讀取的實際程式碼

已重新讀取 `backend/main.py` 第7558-7567行：

```python
# ── 全台股掃描結果 ──────────────────────────────────────────
@app.get("/api/scan/latest")
def get_scan_latest(user: dict = Depends(require_paid_user)):
    scan_path = os.path.join(os.path.dirname(os.path.abspath(__file__)), "stock_picker", "output", "scan_result.html")
    if not os.path.exists(scan_path):
        raise HTTPException(status_code=503, detail="掃描結果每日15:30更新，請稍後再試")
    with open(scan_path, "r", encoding="utf-8") as f:
        html = f.read()
    from fastapi.responses import HTMLResponse
    return HTMLResponse(content=html)
```

**實際行為確認**：第7562行 `if not os.path.exists(scan_path):` 是明確的存在性檢查，若檔案不存在，會在第7563行主動拋出 `HTTPException(status_code=503, detail="掃描結果每日15:30更新，請稍後再試")`，並在 `open()`（第7564行）執行前就已經 `return`（FastAPI的`HTTPException`會中斷函式並直接回傳給客戶端）。**不會**執行到`open()`導致`FileNotFoundError`，也**不會**回傳未處理的500錯誤——回傳的是**受控的503狀態碼＋中文提示訊息**，這是正常、良好的錯誤處理設計。

### 2.3 需要同步修正的其他章節

| 章節 | 原內容 | 修正後 |
| -- | -- | -- |
| 原報告第五節「缺失、斷裂與矛盾引用」表格第1列 | 「讀取不存在的檔案，且**無**存在性檢查／main.py:7561,7564／...可能影響：500錯誤」 | **整列移除**，不應出現在第五節。此項目經重新核對後判定為**正常的防禦性程式設計**，不屬於缺失或斷裂引用。 |
| 原報告第六節「斷裂／矛盾引用數」 | 8 | 移除`scan_result.html`這1項後剩餘7項，經本文件第三節進一步做六類細緻分類，**最終修正為2**（僅②類「真正仍有效的路徑／操作風險」計入，見第三節3.1、3.3） |
| 原報告第六節「最需要人工確認的五項內容」第4項 | 「`backend/main.py:7561-7564`讀取...沒有`os.path.exists`存在性檢查...建議之後確認這是否為已知風險或需要補上防護的程式缺陷」 | **整項撤回**。此項不再需要人工確認或修補，程式碼本身已經是正確、完整的防禦性寫法。原報告「最需要人工確認的五項內容」建議改為只列剩餘4項（原第1、2、3、5項），或由使用者自行決定是否需要補充第五項候選（例如本文件新發現的`check_tools.py`／`fix_tools_batch_v2.py`未被`.zeaburignore`排除一事，見1.3節） |

**移除依據**：本項結論是靜態分析時誤判——原始檢查邏輯（先讀取程式碼片段）沒有完整涵蓋到第7562-7563行的`if`判斷式與`HTTPException`拋出，才會誤以為第7564行的`open()`沒有保護。本次重新完整讀取7558-7567行10行程式碼後確認判斷錯誤，正式撤回。

---

## 三、第五節其餘7項逐一重新核對

### 3.0 問題1優先處理：`enrich_lottery_v3.py` 的 `i18n_data` import 依賴——已直接核對原始碼，不再標記「不確定」

直接開啟 `enrich_lottery_v3.py` 原始碼並搜尋 `import i18n_data` / `from i18n_data import`：

```
enrich_lottery_v3.py:9:import i18n_data
```

**明確答案：有。** 第9行確實是 `import i18n_data`，與 `目前狀態總覽.md:39` 記載的「v3執行時必須import這支檔案」**完全相符**。此項原本因未逐行驗證而標記「不確定」，現已直接核對原始碼確認屬實，不再是不確定項目，也不構成矛盾引用——文件記載與程式碼實況一致。

### 3.1 六類重新分類

依使用者指示，原本「保留/撤回」的二分法改為以下六類。**只有「真正仍有效的路徑／操作風險」這一類才計入第六節「斷裂／矛盾引用數」。**

| 類型 | 對應項目 | 是否計入斷裂／矛盾引用數 |
|---|---|---|
| ① 已撤回的錯誤判斷 | `scan_result.html`（已於第二節處理） | 否 |
| ② 真正仍有效的路徑／操作風險 | README舊路徑指令、一次性腳本寫死本機路徑 | **是（計入，共2項）** |
| ③ 文件資訊落後 | `generate_tools.py`檔名、main.py行數 | 否 |
| ④ 已核實並關閉的歷史疑問 | admin.html路徑待確認、`enrich_lottery_v3.py`+`i18n_data.py`依賴（本次已核對原始碼確認屬實） | 否 |
| ⑤ 文件或環境設定說明不足 | DB_PATH／env.example | 否 |
| ⑥ 尚未完成原始碼核對 | （無，`enrich_lottery_v3.py`已於3.0核對完畢移出此類） | 否 |

**第六節「斷裂／矛盾引用數」修正為：2**（而非原補充報告第一版所寫的7，也不是原報告的8——只有②類的2項屬於「真正仍有效的路徑／操作風險」）。

### 3.2 逐項重新核對明細

| 原報告項目 | 六類分類 | 重新讀取的來源檔案 | 實際行號 | 實際程式碼或設定片段 | 實際檔案狀態 | 重新判定 |
|---|---|---|---:|---|---|---|
| README範例指令指向舊副本路徑 | **②真正仍有效的路徑／操作風險** | `backend/stock_picker/README.md`、`stock_picker/README.md` | 69 | 「## Windows 開機自動啟動」段落，67-73行：建立`run_scheduler.bat`，內容為 `cd /d D:\xian-shang-you-wei\stock_picker` `set FINMIND_TOKEN=...` `set ANTHROPIC_API_KEY=...` `python main_picker.py --schedule` | 已重新讀取兩份README（內容確認SHA-256相同），此指令範例確實指向**根目錄**`stock_picker`（該資料夾依原報告第三、四節已確認是舊副本，main.py只使用`backend/stock_picker`） | **確認有效**——文件內容與main.py實際使用路徑不一致的真實情況。README本身不會被任何程式執行，只有人工依此文件手動設定Windows排程時才會受影響；若真的在根目錄`stock_picker`下設定開機排程，執行的會是**舊副本**的`main_picker.py`，只影響人工手動操作的正確性，不影響main.py本身透過API伺服的執行期行為 |
| 一次性腳本內大量寫死開發者本機路徑 | **②真正仍有效的路徑／操作風險** | `fix_currency_rates.py`、`fix_all_bugs.py`、`fix_converter_dropdowns.py`、`backend/add_tool_charts.py`、`generate_indexes.py`、`move_langbar_top.py`等 | 各檔如原報告2.5節所列行號（例：fix_currency_rates.py第5行） | 重新讀取`fix_currency_rates.py`第1-10行確認：`root = r"D:\xian-shang-you-wei"` `tools = os.path.join(root, "backend", "frontend", "tools")` `if not os.path.isdir(tools): print("找不到 tools 目錄"); sys.exit(1)` | 已重新確認路徑`D:\xian-shang-you-wei`目前確實存在（本機當前專案路徑），腳本能正常執行；且腳本本身有`if not os.path.isdir(...)`防護 | **確認有效**——這些腳本確實寫死本機路徑，換機器/換帳號會導致找不到路徑，雖有基本存在性檢查與提示訊息、不會造成資料損毀，但**執行本身仍會直接失敗**（`sys.exit(1)`），且不被main.py或任何部署流程呼叫，純粹是人工不定期手動執行的維護工具，僅影響「換機器手動執行時能否順利跑起來」 |
| 文件記載腳本已不存在（`generate_tools.py`） | **③文件資訊落後** | `總結.md` | 578, 582, 595 | 578:`**腳本位置：** \`D:\xian-shang-you-wei\backend\generate_tools.py\`` ／ 582-584:`cd D:\xian-shang-you-wei\backend` `set ANTHROPIC_API_KEY=...` `python generate_tools.py` ／ 595:`generate_tools.py 已跑完，350+ 頁 HTML 已產出在 \`D:\xian-shang-you-wei\backend\output\tools\\\`` | 已用`find`對全專案（含所有子目錄）搜尋檔名`generate_tools.py`，**確認完全不存在**；專案內僅有`generate_tools_v2.py`（根目錄，非`backend/`下） | 文件撰寫當下可能確有此檔，後續已被重新命名/替換為`generate_tools_v2.py`但文件未同步更新。**只影響人工閱讀`總結.md`時可能找錯檔案**，`總結.md`本身不會被任何程式或部署流程讀取，**不影響實際部署／執行** |
| 文件與實際檔案大小/行數描述不一致（main.py行數） | **③文件資訊落後** | `架構地圖_線上有位.md` | 10 | 「\`main.py\` \| 後端 FastAPI 主程式（9,397 行）...\| \`D:\xian-shang-you-wei\backend\main.py\`」 | 已重新執行`wc -l backend/main.py`，**目前實際為9,508行**，與文件記載的9,397行相差111行 | 文件標註建立日期2026/07/26，main.py在此後持續有修改，行數自然增加。純屬文件更新落後於程式碼演進的正常現象，**不是程式bug，不影響部署或執行** |
| 文件內路徑待確認之註記（admin.html） | **④已核實並關閉的歷史疑問** | `架構地圖_線上有位.md` | 12 | 「\`admin.html\` \| 獨立管理後台頁面...\| \`D:\xian-shang-you-wei\backend\frontend\admin.html\`（確切路徑待你確認）」 | 已重新確認`backend/frontend/admin.html`**確實存在**（45,653 bytes，2026-07-27修改），且與main.py的服務路徑（`_FRONTEND_DIR`＋catch-all路由）完全一致 | 文件作者留下的「待確認」註記，本次盤點已代為確認完畢，路徑正確無誤，**該擔心的問題不存在，建議從第五節移除** |
| 文件記載路徑與依賴關係（`enrich_lottery_v3.py`+`i18n_data.py`） | **④已核實並關閉的歷史疑問** | `目前狀態總覽.md`；並直接核對`enrich_lottery_v3.py`原始碼 | `目前狀態總覽.md`37-44（核心39行）；`enrich_lottery_v3.py`第9行 | `目前狀態總覽.md:39`「修正方案：\`enrich_lottery_v3.py\` + \`i18n_data.py\`...v3執行時必須import這支檔案」；核對後`enrich_lottery_v3.py:9`確為`import i18n_data` | 見本節3.0，**已直接核對原始碼確認文件記載屬實**，兩檔案都在根目錄、import關係確實存在 | 文件記載與程式碼一致，**不是矛盾引用，建議從第五節移除**，改列為已核實並關閉的歷史疑問 |
| 大小寫/路徑風格（main.py DB_PATH，開發機Windows vs 部署Linux） | **⑤文件或環境設定說明不足** | `backend/main.py`、`backend/env.example` | main.py:71 | `DB_PATH = os.environ.get("DB_PATH", "/data/members.db")` | 此路徑完全由環境變數`DB_PATH`控制，`/data/members.db`只是預設值（fallback）；重新讀取`backend/env.example`確認**其中並未列出`DB_PATH`這個環境變數**（只有`FINMIND_TOKEN`與`ALLOWED_ORIGINS`） | Zeabur正式環境（Linux容器）使用`/data/members.db`是**符合Zeabur持久化硬碟掛載慣例的正常設計**，不構成部署環境的斷裂引用。真正的落差是`backend/env.example`範本**沒有列出`DB_PATH`**，屬於**環境變數範本文件的說明不足**，而非路徑不一致的程式錯誤 |

### 3.3 本節結論

原7項重新分類後：
- **②真正仍有效的路徑／操作風險（2項）**：README舊路徑指令、一次性腳本寫死本機路徑——**這2項計入第六節「斷裂／矛盾引用數」**。
- **③文件資訊落後（2項）**：`generate_tools.py`檔名、main.py行數——不計入斷裂引用數，僅供留意文件已過時。
- **④已核實並關閉的歷史疑問（2項）**：admin.html路徑、`enrich_lottery_v3.py`+`i18n_data.py`依賴——**建議直接從原報告第五節移除**，這兩項經本次核對後確認問題不存在。
- **⑤文件或環境設定說明不足（1項）**：DB_PATH／env.example——不計入斷裂引用數，屬於文件完整性建議。

**沒有任何一項需要像`scan_result.html`那樣完全撤回為「誤判」**（`enrich_lottery_v3.py`+`i18n_data.py`與admin.html兩項不是誤判，而是「原本合理的疑問，經核對後確認無問題」，兩者性質不同，故分類為④而非①）。

---

## 四、原報告章節需要更新的結論與數字彙總

| 位置 | 原內容 | 需更新為 |
| -- | -- | -- |
| 第四節「完全相同的檔案」（**更名為「SHA-256內容相同組數」，見本文件開頭警語**） | 42組 | **1,193組**——分類為A.預期同步副本/部署鏡像971組（不建議視為冗餘）、B.正式資料與備份版本一致211組、C.真正疑似冗餘可考慮清理11組（明細見本文件第一節1.2） |
| 第四節 tools/blog/lottery/glossary/patterns/comparisons 相關比對 | 未逐檔比對（僅列統計） | 已補齊，明細見本文件第一節1.3、1.4 |
| 第五節「缺失、斷裂與矛盾引用」表格 | 8項 | **2項**（`scan_result.html`一項已撤回，見本文件第二節；其餘7項經六類重新分類，只有②「真正仍有效的路徑／操作風險」的2項〔README舊路徑指令、一次性腳本寫死本機路徑〕仍應留在第五節計數，③文件資訊落後2項與⑤說明不足1項建議降級為附註而非「斷裂引用」，④已核實並關閉的歷史疑問2項〔admin.html路徑、`enrich_lottery_v3.py`+`i18n_data.py`〕建議直接移除，詳見本文件第三節） |
| 第六節「完全相同的重複檔案組數」（**更名為「SHA-256內容相同組數」**） | 42 | **1,193**（A.971／B.211／C.11，見1.2節） |
| 第六節「同名但內容不同的檔案組數」 | 13 | 原13組不變，**另加計3,650個服務版本vs備份版本的檔案層級差異**（tools 3,230／glossary 218／blog 110／lottery 92），總計**3,663個檔案層級實例**（詳見本文件1.5節說明，因比對基準不同不宜直接合併為單一「組數」） |
| 第六節「斷裂／矛盾引用數」 | 8 | **2**（僅計入本文件3.1節分類②「真正仍有效的路徑／操作風險」：README舊路徑指令、一次性腳本寫死本機路徑；`scan_result.html`已完全撤回不計，其餘4項降級為文件落後/說明不足/已核實關閉，不再計入此數字） |
| 第六節「最需要人工確認的五項內容」第4項 | `scan_result.html`存在性檢查疑慮 | **整項撤回**，該疑慮已證實不成立 |
| 第六節「最需要人工確認的五項內容」第5項提及的開發紀錄文件落後問題 | 泛指多份文件路徑/行數不一致 | 可明確收斂為本文件③類的2項（`generate_tools.py`檔名、main.py行數），④類2項已確認無問題可從關注清單移除 |

---

## 五、補充後仍然無法確認的項目

> 原第1項（`enrich_lottery_v3.py`是否import `i18n_data`）已於本文件第三節3.0直接核對原始碼確認「有」，不再是無法確認項目，已移除。

1. **`backend/output/glossary/`、`knowledge-engine/content/`、`tools_backup/`、`output/tools/`等目錄的確切產生時間點與先後順序**——本次僅能透過檔案mtime與內容比對得知「彼此不同」，但無法單靠靜態分析判斷正確的世代先後關係（例如`tools_backup`與`output/tools`何者較舊），需要人工回憶或查看檔案系統以外的紀錄（如部署日誌）才能確認。
2. **`backend/frontend/tools_backup_charts/check_tools.py`與`fix_tools_batch_v2.py`是否真的被Zeabur部署打包進正式環境**——本次僅能確認`.zeaburignore`規則沒有涵蓋到這兩個檔名，但無法在不存取Zeabur後台的情況下確認實際部署容器內是否存在這兩個檔案。
3. **Zeabur正式環境的`DB_PATH`環境變數目前實際設定值**——本機無法讀取Zeabur儀表板的環境變數設定，只能確認程式碼邏輯與`env.example`範本內容，無法確認正式環境是否已經正確設定此變數。

---

**執行限制確認**：本次補充作業僅新增本文件`專案結構盤點_雜湊補充報告.md`一份檔案，未修改、移動、刪除或重新命名任何既有檔案（含`project_structure_inventory.md`本身），未執行任何格式化、建置、部署、套件安裝或資料庫migration，未變更Git狀態。所有暫存雜湊清單與分析腳本均存放於系統暫存目錄，未留在專案內。
