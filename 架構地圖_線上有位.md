# 線上有位 / SoftGlow 檔案架構地圖
> 建立日期：2026/07/26　目的：記錄「哪個功能寫在哪支檔案」，避免日後找不到程式碼位置

---

## 核心檔案清單

| 檔案 | 用途 | 本機路徑 |
|------|------|---------|
| `main.py` | 後端 FastAPI 主程式（9,397 行）：台股分析引擎、會員/JWT、綠界金流、多語靜態頁路由（tools/lottery/blog/patterns）、論壇、排程、admin API | `D:\xian-shang-you-wei\backend\main.py` |
| `index.html` | 前端主 SPA（5,700+ 行）：一般會員/訪客看到的網站主體，含登入、分析、自選股、論壇、我的帳號 | `D:\xian-shang-you-wei\backend\frontend\index.html` |
| `admin.html` | 獨立管理後台頁面：會員管理、密碼重設、備份、開通帳號等 | `D:\xian-shang-you-wei\backend\frontend\admin.html`（確切路徑待你確認） |

---

## 認證/金鑰機制對照表（2026/07/26 修正後）

| 用途 | 環境變數名稱 | 使用位置 |
|------|------------|---------|
| 一般會員登入 token 簽章 | `JWT_SECRET` | main.py（`_sign_token` 等函式） |
| 管理後台 API 存取（`/admin/*`） | `ADMIN_API_KEY`（新，取代舊的 JWT_SECRET 前16碼） | main.py `_check_admin()`；admin.html 登入後存 sessionStorage |
| 管理員帳號本身的登入密碼 | 存在資料庫（雜湊），透過 `/admin/reset-password` API 重設 | index.html 一般登入表單、main.py members 表 |

**重要：這三個是三件不同的事，不要搞混：**
1. 一般會員登入用的是「帳號密碼」，驗證通過後發一個 JWT token（用 `JWT_SECRET` 簽章）
2. `admin.html` 這個管理後台頁面，呼叫 API 時帶的是 `ADMIN_API_KEY`，這個 key 現在要在 admin.html 登入畫面手動輸入，不再寫死在檔案裡
3. 管理員帳號（`watione@yahoo.com.tw`）在 index.html 一般登入框輸入的密碼，是資料庫裡存的雜湊值，跟上面兩個 key 都無關

---

## 2026/07/26 安全性修正記錄

| 問題 | 位置 | 修法 | 狀態 |
|------|------|------|------|
| Threads App Secret 寫死在原始碼 | main.py 第 74 行 | fallback 清空，功能已暫停 | ✅ 已修（本次對話輸出） |
| 管理員預設密碼 630428 寫死 | main.py 第 2658 行 | fallback 清空，未設定就跳過建帳號 | ✅ 已修（本次對話輸出） |
| 管理後台金鑰共用 JWT_SECRET 前16碼 | main.py `_check_admin()` | 改用獨立 `ADMIN_API_KEY` | ✅ 已修（本次對話輸出） |
| admin.html 寫死金鑰+密碼在前端原始碼 | admin.html 第 304-305 行 | 改成登入時輸入，存 sessionStorage，後端驗證 | ✅ 已修（本次對話輸出） |
| **index.html 公開網站有管理員帳密自動登入後門**（點 Logo 5 次） | index.html 第 4636-4646 行 `adminTap()` | 移除帳密自動填入，只保留開啟登入視窗 | ✅ 已修（本次對話輸出） |
| TWSE 請求 SSL 驗證關閉 | main.py 第 32-34 行 `_TWSE_SSL_CTX` | 尚未修，範圍限定在抓 TWSE 報價兩處呼叫 | ⏳ 待處理（優先度較低） |

---

## 部署前必做（依序）

1. **去 Zeabur 設定新環境變數 `ADMIN_API_KEY`**（一長串隨機字元），沒設會導致 `/admin/*` 全部無法使用
2. 部署這次修改後的 `main.py` + `admin.html` + `index.html` 三支檔案
3. 部署後測試：
   - 一般會員登入、分析報告功能正常
   - admin.html 用新的 `ADMIN_API_KEY` 能正常登入、操作
   - 到公開首頁，開發者工具搜尋原始碼，確認找不到任何密碼字串
4. 用 `/admin/reset-password`（admin.html 的「重設密碼」功能）把 `watione@yahoo.com.tw` 的密碼從 `630428` 改掉

---

## 之後任何新對話，建議這樣開始

把這份文件、`main.py`、`index.html`、`admin.html` 放進 **Project 檔案區**（不是每次對話的上傳，是 Project 設定裡的永久檔案區），這樣不管開啟哪個新對話，我都能直接看到最新架構，不用每次重新問你檔案在哪裡。
