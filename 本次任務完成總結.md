# 本次任務完成總結（2026-08-07）

給新對話 Claude 看的背景說明：以下 6 項是使用者確認過的改動，已經全部完成、測試、commit、push 上 `origin/main`。

## 已完成的 6 項

| # | 項目 | 改的檔案 | 重點 |
|---|---|---|---|
| 1 | K棒「突破隔日拉回」敘事 | `backend/main.py` | 孕線出現時，若前一天有 `breakout_idx`/`breakdown_idx` 訊號，改用「昨日已放量突破/跌破，今日拉回整理」動態文案取代原本靜態孕線文字 |
| 2 | 現價欄位補漲跌%＋日期 | `backend/main.py`、`frontend/index.html` | 新增 `price_change_pct`，用最近兩根收盤價算，不依賴即時報價；日期顯示在「現價」欄位旁 |
| 3 | 深度選股閘門重訂 | `backend/stock_picker/finmind_filter.py` | 月季線金叉＋站上月線是硬條件；MACD金叉從強制門檻改成加分項（+2分）。同步更新了 `generator.py`、`homepage.html`、`admin.html` 裡過時的「三重確認」文案 |
| 4 | 選股紀錄表＋去重＋20天報酬追蹤 | `backend/main.py`、`backend/stock_picker/generator.py` | 新增 `deep_pick_log` 資料表，靠 `UNIQUE(stock_id, cross_date)` 讓同一次金叉事件只記錄一次；背景任務每天檢查滿20交易日的紀錄並自動算報酬率；公開頁新增「選股成效追蹤」表格 |
| 5 | 深度選股卡片瘦身 | `backend/stock_picker/generator.py` | 移除卡片內自算的支撐/壓力/停損，改成連到 `/report/{stock_id}` 完整個股報告的按鈕，避免兩套數字打架 |
| 6 | 全欄位白話說明 | `backend/main.py`、`frontend/index.html` | App分析頁與報告頁的現價、趨勢、型態、支撐、壓力、防守位、損益比、多空雷達四格、K棒型態、動能指標、風險評估、操作建議，都加了一行小灰字白話說明 |

## 額外處理（過程中發現、不在原本6項內）

1. **query_log SQL bug（已修復）**：訪客（未登入）分析股票時，`ON CONFLICT` 欄位跟實際 `UNIQUE(member_id, date, ip)` 限制不一致，導致 100% 報 500 錯誤。已修正於 `backend/main.py:4262`。
2. **FinMind Token 洩漏（已部分處理）**：`.claude/settings.local.json` 的 allow-list 裡有一組真實 FinMind Token 明碼被 commit 進 git 歷史。已從目前檔案移除、加入 `.gitignore` 避免再犯。**但舊 commit 歷史仍含這組 token，需要使用者自行到 FinMind 後台撤銷重發，這是唯一能讓外洩 token 真正失效的方法**（改檔案無法消除 git 歷史）。

## 驗證方式

- 全部改動 Python 檔案都跑過 `py_compile` 語法檢查。
- 項目 3、4（邏輯最重）用合成 K 線資料做過端到端模擬測試：閘門邏輯（MACD不再是硬門檻）、選股記錄去重、20天報酬率計算、公開頁 HTML 產出，全部驗證通過。
- 項目 1、2、6 用本機啟動的前後端（backend :8000 + frontend :8080）加上 DOM 層級檢查驗證：App頁14個、報告頁16個小灰字說明全部正確渲染、無跑版溢出。
- 本機測試伺服器已於驗證完後關閉。

## Commit 記錄

```
c793bfac security: 移除已洩漏的 FinMind Token、settings.local.json 改為 gitignore
aef186de feat: 深度選股閘門重訂、選股紀錄追蹤、K棒敘事優化、全欄位白話說明
```

兩個 commit 都已 push 上 `origin/main`（`fb00e2ac..aef186de`）。

## 還沒做、需要使用者自己處理的事

- **FinMind Token 撤銷重發**：git 歷史裡舊 commit 仍含明碼 token，必須到 FinMind 後台手動撤銷、產生新 token，並更新正式環境（Zeabur）的環境變數。
- 沒有對 `deep_pick_log` 這張新表做過正式環境的實際資料驗證（測試都是合成資料），建議下次 17:00 排程跑完後，抽查一下這張表的資料是否合理。
