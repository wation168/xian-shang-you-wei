# SoftGlow 新工具/新頁面規劃守則

> 這份文件是「設計新工具或修改既有工具前」的檢查清單，由歷次實際踩過的bug整理而成。
> **這是活文件**——之後每次發現新的同類型錯誤，都要補進對應章節，不要重新開一份。
> 更新原則：跟《目前狀態總覽.md》一樣，每次有新發現主動更新，帥哥鴻覆蓋上傳Project檔案區。

---

## 一、貨幣/金額欄位設計守則

### 1-1 先問：這個工具的「錢」屬於哪個地理範圍？
設計任何涉及金額的工具前，先判斷是下面哪一種，判斷結果會決定整個工具怎麼做多語言：

| 類型 | 特徵 | 處理方式 |
|---|---|---|
| **國家專屬制度** | 全世界只有這個國家有這個東西（如美國401k/HSA、英國印花稅、台灣勞基法資遣費） | 10語言版本用同一套數字，只翻譯文案，貨幣符號/JS計算邏輯全部維持該國幣別，不隨頁面語言變動 |
| **通用概念、各國有自己數據** | 每個國家都有（如碳排放係數、房貸利率、油價），但實際數字/規則因國而異 | 需要真實查證各國數據，不能用同一套數字套多國；如果一個語言代碼涵蓋多國（見1-3），要做地區選單 |
| **通用公式、跟國家無關** | 純數學/比例計算，沒有具體法規綁定（如DCF估值、加薪百分比、淨資產×風險係數） | 只要換貨幣符號+locale格式化就好，不用查證任何國家法規 |

**這次的教訓**：`ev-vs-gas.html`、`loan-vs-lease.html`混合了「國家專屬事實」（美國聯邦EV補助$7,500）跟「通用概念」（油價、車價）在同一個工具裡，導致批次修正時很容易顧此失彼——通用欄位要跟著在地化，專屬事實欄位不能被症狀式的符號替換覆蓋掉。**混合型工具在設計階段就要把「哪些欄位是專屬事實、哪些是通用」標記清楚**，寫在程式註解或欄位命名規則裡（例如專屬事實欄位id加`_usOnly`字尾），方便之後任何批次腳本自動識別排除。

### 1-2 JS計算邏輯 = 輸入框符號 = 內文舉例金額所屬國家，三者必須一致
本次全部bug的共同根因都是這三者其中之一沒對齊：
- JS計算結果`textContent = '$' + X.toLocaleString('en-US', ...)`寫死美金格式，沒跟著頁面語言換
- 輸入框`<span class="input-prefix">`已經翻譯成當地符號，但JS算出來的東西還是美金——**畫面顯示一種貨幣，結果卻是另一種**，比完全沒翻譯更危險
- 內文（article/FAQ）舉具體例子時（如「最高可達$7,500」），翻譯時只換了貨幣符號卻沒管這個數字背後的國家事實是否還成立（例：把美國聯邦補助的€7.500配上「en los Estados Unidos」，貨幣符號跟文字自相矛盾）

**新工具開發完成後，批次抽查這三者是否一致，是上線前的必要檢查項目。**

### 1-3 語言代碼涵蓋多國時，要在設計階段先決定策略
`pt`（葡萄牙文）同時對應葡萄牙（€）和巴西（R$，人口是葡萄牙20倍以上）、`es`（西班牙文）涵蓋西班牙與拉美多國，都是同一個問題的不同案例。設計時先問：

1. 這個工具的底層計算邏輯/法規在兩國之間**有沒有實質差異**？
   - 沒有差異（純貨幣/單位換算，如油費、房屋折舊）→ 可以做地區下拉選單，用JS動態切換符號+locale
   - 有實質差異（稅制、保險市場、勞動法，如雇主成本、車險估價）→ **不能只做symbol切換**，需要先查證兩國真實法規/費率數據，數據沒查到位前先統一單一國家，不要做半套的下拉選單
2. 下拉選單的UI風格要跟既有案例（`carbon-footprint.html`）一致，不要自己發明新樣式

---

## 二、避免時效性維護負擔的設計原則

**新工具/新頁面優先選「通用型、翻譯好就能上線、之後不用回頭維護」的題材，盡量避開「內建會過期的具體數字」這種設計。**

### 2-1 為什麼要避開時效性內容
凡是工具內容綁定「今年/本年度的具體數字」（稅率級距、扣除額上限、政府補助金額、法規門檻），就等於幫自己埋了一個「明年一定要回來改」的地雷，而且是**10種語言乘以每個綁時效性數字的工具**，維護成本會隨語言數量疊加。已發生的實例：401k/HSA/FSA/Roth退休金試算機50個檔案(5模板×10語言)，2026年IRS額度一出來就要全部手動更新一輪，且過程中還額外挖出zh-CN/韓文版數字早就跟其他語言對不上的舊錯誤——**這類工具每年都要重複同一輪「查證+批次更新+跨語言核對」的工程**。

### 2-2 設計新工具時的判斷順序
1. **這個工具的核心邏輯，會不會因為「換了一年」而答案不一樣？**
   - 會（稅率級距、政府補助額度、法定門檻）→ 優先考慮**不做**，或者做成「使用者自己輸入當年度數字」的通用計算機，不要把具體數字寫死進工具裡
   - 不會（數學公式、比例換算、通用金融方法論如DCF/複利、單位換算）→ 適合做，翻譯完就能長期上線不用回頭維護
2. 如果一定要做時效性內容（例如401k這種使用者高度期待「幫我算好」而非「自己查數字」的工具），**要有清楚的資料來源標註+更新日期**，而且要接受這是需要定期排入待辦的維護項目，不是做完就結束

### 2-3 「通用型」的具體判斷標準
好的通用型設計範例：使用者輸入自己的稅率/費率/門檻數字，工具只負責運算邏輯——這樣公式本身沒有時效性，10語言只要翻譯文案跟UI就好，不用管數字每年變。
壞的範例（已發生過的教訓）：`work-days.html`原本標榜「2026年版」但實際上完全沒有內建假日資料庫，是無依據宣稱；如果真的要做「內建當年度假日」這種功能，就是把時效性維護成本綁進來，要謹慎評估是否值得。

### 2-4 這條原則也適用於「跟時間相對」的敘述
不只是具體數字，文案裡「今年」「最新」「2026年版」這類相對時間詞，如果背後沒有真的隨時間更新的機制，也是同類型風險（宣稱與實際內容不符），設計時盡量用中性敘述（如「您輸入的數字」「當前有效稅率」）取代寫死的年份宣稱。

---

## 三、生成腳本（generate_tools_v2.py等）設計守則

- **AI翻譯負責的不只是文案，連「這個工具的錢/單位屬於哪個地理範圍」這個語意本身都可能翻譯不到位。** `tr_labels()`函式「AI回傳跟原文相同視為沒翻譯」的判斷邏輯本身有漏洞（有些語言剛好翻出來的字長得跟原文一樣，不代表沒翻譯；反過來AI也可能誤翻不該翻的專屬事實）
- **批次生成後，务必抽查「輸入框符號、JS計算符號、內文舉例金額所屬國家」三者是否一致**（見1-2），這是本次全部bug的共同模式，不是個案
- 純符號/字典可以處理的翻譯（貨幣符號、單位名稱），優先用regex/字典對照表處理，不要動用AI生成（帥哥鴻已反饋過API成本疑慮）
- CATS分類字典（`more_pills()`用）等「工具屬於哪個分類」的登記，必須是生成新工具時**強制同步寫入**的步驟，不能分開兩支腳本各自維護，否則容易漏登記（目前已知206個工具漏登記，待修）

---

## 四、批次修正腳本安全守則（延續既有HARD RULE，這次新增的部分）

- **判斷「這是不是bug」不能只看「跟語言預設是否相符」**，要看「JS計算結果、輸入框、內文」三方是否互相矛盾。三方一致但跟頁面語言常態不同（如英文版顯示台灣NT$的資遣費工具、UK印花稅顯示£），是合法的國家專屬內容，**不是bug**，批次腳本必須先建立「已知例外清單」（美國制度、UK制度、台灣制度等）再開始跑，不能等出事才補
- **正規表示式的符號清單要用同一份來源產生，不要分開手key**——這次曾發生正規表示式漏了`£`符號導致UK印花稅頁被誤判要改成美金，根源是分類腳本跟修正腳本各自維護一份符號清單，沒同步更新
- 腳本回報「找不到pattern/已完成」時，**要先跟線上實測交叉驗證，不能只信終端機輸出就結案**——這次曾誤判「no_pattern_found」代表壞掉，實際上是「已經成功套用過，重跑找不到pattern」的正常保護機制，靠連瀏覽器查詢即時狀態才釐清狀況
- **對批次操作有疑慮時，先用3-10個代表性樣本本機測試**（含用node --check驗證JS語法、模擬DOM執行驗證計算結果正確），驗證通過才交給帥哥鴻在正式環境跑，不要跳過這步直接全站套用

---

## 五、驗證方式守則

- 任何「已修復/已部署」的宣稱，一律要**連瀏覽器實測活的網站**（不是看程式碼邏輯推論、不是看終端機訊息就結案），且要用cache-busting參數（`?_cb=xxx`）避開CDN快取看到最新版本
- 修正涉及JS計算邏輯時，光看程式碼「看起來對」不夠，要**用node實際執行驗證**（模擬DOM環境、餵入測試值、確認輸出結果符合預期），這次驗證出好幾次「看起來對但邏輯有誤」的案例
- 內文裡的「具體事實宣稱」（金額、稅率、年份、資料來源）比「用詞在地化」更需要嚴格查證到底層邏輯——不能只看文字表面翻得順不順

---

## 六、待處理清單（持續累加）

- [ ] CATS字典漏登記206個工具（63.8%），導致`get_cat()`fallback成"finance"錯誤推薦
- [ ] 「更多工具」連結顯示原始英文slug，根因尚未查出（追查`generate_tools_v2.py`三個候選函式皆對不上來源）
- [ ] LOCALIZE 357筆（加侖/英里等美制單位換算成公升/公里），規則已定案（數學換算不用查真實市場價格），腳本尚未寫
- [ ] `pt/home-energy-audit.html`設備名稱清單「Air Conditioning」未翻譯成葡萄牙文
- [ ] `untranslated_report.csv`剩餘類別（後綴疑似未翻譯400筆、標籤/後綴常見英文字19筆）尚未處理
- [ ] 目前只查過`freelancer-tax`/`property-tax`/`oop-maximum-calc`等24個檔案的JS未在地化問題，**不保證是全部**，之後若還有工具出現「顯示地區符號正確但算出來是別的幣別」的情況，優先懷疑同一種病灶

---

## 附：本文件緣起

2026/08/10（第十九輪）財經工具站貨幣/在地化bug全面體檢時，累積踩過的錯誤模式歸納而成。完整修復記錄見《目前狀態總覽.md》第十九輪章節。
