# SoftGlow / 線上有位 — 目前狀態總覽
> 這份文件是「單一入口」。每次新對話開始，先讀這份，不用帥哥鴻重講背景。
> 更新原則：每次有實質修改/發現，Claude 主動重新輸出這份文件，帥哥鴻負責覆蓋上傳到 Project 檔案區取代舊版。
> 2026/07/27 這次更新：任務B（手動觸發深度選股CORS問題）與任務A（彩票站內容盤點+修正）都已完成並部署。

---

## 🎯 新對話建議從這裡開始

1. 先讀這份文件全部內容
2. 確認下方「⏳ 立即待辦」有沒有已經完成
3. 沒有新的緊急事項的話，就接續「📋 待辦事項」清單繼續做

---

## ✅ 2026/07/27 完成的事

### 任務 B：手動觸發深度選股 CORS 問題 — 已完成部署
- **根因確認**：main.py 的 CORS 設定（`ALLOWED_ORIGINS` 環境變數）只允許正式網域，本機 `file://` 開啟的 HTML 送出的 Origin 是 `null`，先天就不會在白名單裡，不管怎麼調整白名單都解決不了。
- **修法**：不動 main.py，直接把觸發按鈕整合進 `admin.html`（跟現有「開盤熱門股」按鈕同一套模式：`getAdminKey()` 讀 sessionStorage 金鑰、同源呼叫 API）。
- **交付內容**：`admin.html` 左側新增「🎯 深度選股」導覽項，卡片內按鈕呼叫 `POST /admin/run-deep-analysis`。已跑過 JS 語法檢查、id唯一性檢查、diff確認只改動這一處。
- **狀態**：已提供給帥哥鴻部署。

### 任務 A：彩票站全面內容盤點 + 語言bug修正 — 已完成部署
**盤點結果（用實際7個範例檔案比對3支腳本邏輯得出）：**

| 頁面類型 | 加厚狀態（修正前） |
|---|---|
| 介紹 intro | 有FAQ，但 ja/ko/fr/de/es/pt/id 七種語言其實是**英文內容**（重大bug） |
| 結果 results | 同上 |
| 歷史 history | 完全沒有FAQ/Schema（原始設計範圍沒涵蓋） |
| 統計 statistics | 完全沒有FAQ/Schema（同上） |
| 共用選號工具（12個） | 正常，骨架生成時就內建FAQ，不用動 |
| 通用選號入口 number-generator.html | meta description是空字串（bug）+ 無FAQ |
| 彩票站首頁 index.html | 純目錄頁，無任何說明文字 |

**重大發現：** `enrich_lottery_pages.py`(v1) 和 `enrich_lottery_v2.py`(v2) 的 `gen_faq()` 語言判斷只有 `zh` / `else`(英文) 兩分支，導致 ja/ko/fr/de/es/pt/id 七種語言的 intro/results 頁 FAQ 實際上是英文，不是在地化內容。稅務/領獎欄位也固定塞英文原文。10語言SEO策略在這塊實際上只有3種語言（zh-TW/zh-CN/en）是真的做了在地化。

**修正方案：`enrich_lottery_v3.py` + `i18n_data.py`（新增檔案，放在 `D:\xian-shang-you-wei\` 根目錄，跟原本v1/v2腳本同層）**
- 補齊 15彩種 × 7語言的稅務/領獎資訊翻譯表 + FAQ題目模板 + howto步驟模板
- 新增「偵測舊版錯誤語言FAQ」機制：用英文特徵字串（"How do I play"等）偵測 ja/ko/fr/de/es/pt/id 頁面裡混入的英文FAQ，抓到就先移除舊區塊再重新注入正確語言版本，不會重複疊加
- 新增 history、statistics 頁面的專屬FAQ（3題，跟intro/results內容不同，避免重複內容風險）
- 修正 number-generator.html 空白 meta description bug，加介紹段落+3題FAQ（10語言）
- index.html 加一段站台介紹文字（10語言，純目錄頁不加FAQ）
- 內建 dry-run 模式（預設安全模式，不加 `--apply` 不會寫檔）+ 執行前自動備份整個 lottery 資料夾
- 已用帥哥鴻提供的7個實際檔案 + 模擬的7語言測試案例完整測試過，確認：HTML結構正確、JSON-LD語法正確、無重複疊加、語言bug確實修好

**實際執行結果（帥哥鴻已跑完 `--apply`）：**
```
Lottery pages: 108 total, 72 enriched
Hub pages enriched: 20
Done!
```
- 72頁 = 9個受語言bug影響的「彩種×語言」組合 × 4種頁面類型（36頁）+ 18個組合（zh-TW/en）× history+statistics新涵蓋（36頁）
- 20個目錄頁 = 10語言 × (number-generator.html + index.html)
- 執行前已自動備份到 `backend\frontend\lottery_backup_時間戳記`

**⏳ 待帥哥鴻驗證（下次對話先確認這幾點做完了沒）：**
1. 抽查受影響語言頁面（例如 `de/euromillions.html`、`ja/japan-loto6.html`）確認FAQ真的顯示對應語言，不是英文
2. 用 Google 結構化資料測試工具（https://search.google.com/test/rich-results）檢查一兩頁 Schema 有沒有正常通過
3. 部署上線後，去 GSC 對這批頁面送出「要求建立索引」

---

## ✅ 2026/07/26 對話完成的事（重點摘要，完整技術細節見 memory edit #14）

### 上半場：GSC JSON Schema bug 修復
- GSC 回報 `/tools/ja/asphalt-calculator.html` 結構化資料解析錯誤
- 查出根因：`thicken_ja_pages.py`（7/25加厚腳本）的 regex 插入點抓錯，導致 FAQ Schema JSON 語法壞掉
- 影響範圍不只 1 頁，同批加厚的 **7 個 ja 頁面全部中招**：asphalt-calculator, apr-calculator, caffeine-calculator, grade-calculator, paper-weight, sharpe-ratio, tile-calculator
- 7 個頁面 + 腳本本身的 bug 都已修復並部署，用 Google 測試工具驗證通過

### 下半場：finmind_filter.py 疑點解決 + 深度選股全面重建
- **疑點解決：** 確認 main.py import 的是 `backend/stock_picker/finmind_filter.py`，該檔案曾在某次「V3 deploy」大重構時被誤刪，7/24 補救時只救回一半（SEO篩選部分），深度選股部分（`_fetch_fundamentals`、`run_deep_scan`）從 git 歷史挖出來復原
- **順帶發現：** `generator.py` 也缺 `generate_deep_analysis` 函式（同一事件的第二個受害者），已補上
- **既然要修，順便整個重新設計深度選股邏輯**（三重確認：月季線金叉、股價站上月線、MACD金叉）：
  - 三重確認門檻 + cond1/cond2/cond3 計分制 + 停損建議 + 風險等級
  - 公開版 `/deep-analysis` 改成顯示「延遲一交易日」資料（當SEO/導流用，3-5則真實ID廣告+加厚SEO內容）
  - App內新增即時版（跑馬燈提醒 + 站內頁面，免費看信心/風險，付費解鎖股票代號）
  - 首頁、介紹頁都加了入口，介紹頁順便修正了 3 處還在講舊版「KD金叉」邏輯的過時文案，以及一個既有的 HTML 標籤缺陷（非本次改動造成）

**這次改動的檔案，總共 6 支待部署：**
```
main.py
stock_picker/finmind_filter.py
stock_picker/generator.py
frontend/index.html
frontend/homepage.html
frontend/landing.html
```

---

## ⏳ 立即待辦（依優先順序）

1. **驗證彩票站72頁修正結果**（見上方任務A的「待帥哥鴻驗證」3點）
2. **部署 7/26 那 6 支深度選股相關檔案**（如果還沒部署）
3. 部署後測試：App內跑馬燈有沒有出現、站內深度選股頁面模糊/解鎖邏輯正常、公開版 `/deep-analysis` 正常顯示
4. 驗證深度選股 17:00、開盤熱門股 09:06 是否正常運作
5. **部署任務B的 admin.html**（如果還沒部署），測試手動觸發深度選股按鈕能不能正常運作

---

## 📁 檔案架構

| 檔案 | 路徑 | 狀態 |
|------|------|------|
| `main.py` | `D:\xian-shang-you-wei\backend\main.py` | 主程式，9,500+行 |
| `finmind_filter.py` | `D:\xian-shang-you-wei\backend\stock_picker\finmind_filter.py` | ⚠️ 注意：根目錄下也有一份同名的 `D:\xian-shang-you-wei\stock_picker\finmind_filter.py`，那份不會被部署，是舊副本，不要搞混 |
| `crawler.py` | `D:\xian-shang-you-wei\backend\stock_picker\crawler.py` | 488行，已確認具備深度選股需要的所有函式 |
| `generator.py` | `D:\xian-shang-you-wei\backend\stock_picker\generator.py` | 產出層，這次新增 `generate_deep_analysis` |
| `main_picker.py` | `D:\xian-shang-you-wei\backend\stock_picker\main_picker.py` | 已確認無問題，只走 SEO 篩選路線，不涉及深度選股 |
| `index.html` | `D:\xian-shang-you-wei\backend\frontend\index.html` | 前端主 SPA |
| `homepage.html` | `D:\xian-shang-you-wei\backend\frontend\homepage.html` | 全站首頁（content-first設計） |
| `landing.html` | `D:\xian-shang-you-wei\backend\frontend\landing.html` | 台股分析介紹頁 |
| `admin.html` | `D:\xian-shang-you-wei\backend\frontend\admin.html` | 管理後台，7/26修過安全性問題，7/27新增手動觸發深度選股功能（已交付待部署） |
| `generate_lottery_final.py` | `D:\xian-shang-you-wei\`（根目錄） | 彩票頁面骨架生成，1328行 |
| `enrich_lottery_pages.py` | `D:\xian-shang-you-wei\`（根目錄） | 彩票加厚 v1，361行，已有語言bug（保留供對照，不再使用） |
| `enrich_lottery_v2.py` | `D:\xian-shang-you-wei\`（根目錄） | 彩票加厚 v2，同樣有語言bug（保留供對照，不再使用） |
| `enrich_lottery_v3.py` | `D:\xian-shang-you-wei\`（根目錄） | ✅ 彩票加厚 v3（2026/07/27新增，已執行部署），修好語言bug+涵蓋history/statistics/hub頁 |
| `i18n_data.py` | `D:\xian-shang-you-wei\`（根目錄，跟v3同層） | ✅ v3腳本的多語言翻譯資料表（15彩種×7語言），v3執行時必須import這支檔案 |

**⚠️ 這個表格還不是100%完整盤點**，例如 `.github/`、`scripts/` 資料夾、CLAUDE.md 內容都還沒細看過，如果之後又卡在「檔案對不上」的問題，先用 `git log --all --oneline -S"函式名"` 查有沒有歷史版本，比憑空猜測可靠。

---

## 🔑 認證/金鑰機制對照表

| 用途 | 環境變數名稱 | 使用位置 |
|------|------------|---------|
| 一般會員登入 token 簽章 | `JWT_SECRET` | main.py |
| 管理後台 API 存取（`/admin/*`） | `ADMIN_API_KEY` | main.py `_check_admin()`；admin.html 登入後存 sessionStorage |
| CORS 允許來源 | `ALLOWED_ORIGINS`（逗號分隔） | main.py，未設定時開發環境預設允許localhost |
| 管理員帳號登入密碼 | 存資料庫（雜湊） | ⚠️ 待確認帥哥鴻是否已用 admin.html 重設密碼功能把密碼從 630428 改掉 |

## 🎯 AdSense 廣告單元ID

全站目前只有 **3 個真實廣告單元ID**（帳號 `ca-pub-1768270548115739`）：
- `2793159185`（main.py article區用）
- `4182262477`（main.py bottom區用；homepage.html K線型態區塊也用這個）
- `1655301946`（homepage.html 平台服務區塊上方用）

深度選股頁的 3-5 則廣告目前交替使用前兩個ID（跟全站共用，數據會混在一起算，之後流量穩定可以考慮申請專屬ID分開追蹤成效）。

---

## 🌐 兩大網站現況

### softglow-ai.com
- 總頁數 ~8,633，GSC 28天：118 點擊、10,341 曝光、平均排名 22.06、CTR 1.14%
- 深度選股功能整個重建（見7/26記錄）
- SEO 加厚 38+7=45 頁（原38頁 + 7個ja頁修復）
- 209 頁「已檢索未索引」待處理

### 彩票站
- 靜態遷移：248 靜態頁已生成，main.py `/lottery/` 路由 + 301 redirect + GitHub Actions 日更 cron 待完成部署（狀態同之前，這次沒動）
- **內容深度：** ✅ 已完成全面盤點與修正（見上方任務A），108個彩票頁+20個目錄頁已加厚部署，待驗證實際上線效果

---

## 📋 待辦事項（按優先順序）

1. 驗證彩票站72頁修正結果（語言正確性、Schema驗證、GSC送索引）
2. 部署 7/26 深度選股相關6支檔案（如果還沒部署）
3. 部署 admin.html 手動觸發深度選股功能（如果還沒部署）
4. 驗證深度選股/開盤熱門股排程正常運作
5. GSC送出剩餘5個ja頁面索引請求
6. 等帥哥鴻註冊 Fiverr Affiliate + Pinterest 商業帳號後開始圖卡製作
7. 8/2 追蹤38個加厚頁面排名變化
8. 8/9 追蹤全站收錄率
9. main.py 12模組拆分（等深度選股穩定+有空檔，用 Fable 5）
10. 彩票替代資料源整合
11. 食譜頁面開發
12. 考慮深度選股頁申請專屬AdSense單元ID
13. lottery.softglow-ai.com → /lottery/ 靜態遷移的 main.py 路由+301+cron（原本待辦，這次盤點沒動到）

---

## ⚠️ 給我自己（Claude）的提醒

- **「main.py import 了但檔案裡沒有」這種矛盾，git歷史通常能給答案。** 用 `git log --all --oneline -S"函式名"` 找函式存在過的commit。
- **一個檔案缺函式，通常不會只有一個檔案受害**，修的時候要主動檢查呼叫鏈上下游有沒有同樣問題。
- **批次生成腳本一定要做完就跑語法驗證**，不要等 Google 爬到才發現壞掉。
- **改動涉及金錢/廣告/付費邏輯時，要主動去找已經存在的真實設定值，不要用佔位符然後叫帥哥鴻自己填。**
- **只要這次對話有實質修改/進度變化，不用帥哥鴻開口，就要主動重新輸出這份文件並提醒他覆蓋上傳。**
- **發現問題根因後，不要急著只修表面症狀，先確認帥哥鴻是否要完整盤點再統一處理。**
- **【7/27新教訓，非常重要】「有沒有這個東西」跟「這個東西對不對」是兩個不同的檢查，不能只用存在性判斷（如 has_real_faq）來決定要不要重新處理，否則已經壞掉的舊資料會被永久跳過、bug修了等於沒修。修bug時要想：這個判斷式會不會把「已經壞掉但表面上看起來有東西」的情況誤判成「沒問題」？**
- **dry-run跑出來的數字要自己動腦驗算一次再讓帥哥鴻執行**，不能只看「有跑出結果」就當作沒問題，這次就是靠自己算了一遍 108/4×2=54 才發現語言bug漏修。
- 帥哥鴻是 IT 小白，指令要能直接複製貼上，路徑要寫完整。
- **重要限制：Claude 沒有工具能自動把檔案寫進 Project 檔案區，只有帥哥鴻能點上傳。**
- **不要用「應該還沒上線/還沒部署」這種臆測語氣描述帥哥鴻的網站狀態**，網頁搜尋抓不到不代表沒上線，帥哥鴻已經明確糾正過這點（7/27），有疑問要直接問，不要用猜測的口氣陳述。

---

## 🗂 其他歷史文件（僅供查閱細節，日常不用全部重讀）

- `PROJECT_BRIEFING.md`、`總結.md`、`全球彩票總結.md`、`SoftGlow_策略演進對照表.md`、`SoftGlow_Production_Blueprint_v1.md`、`v8_14_session_summary.md` — 早期規劃與版本演進記錄，內容偏歷史脈絡，實際「現在是什麼狀態」以這份文件為準
- `帳號密碼範本.md` — 帥哥鴻自己保管的敏感帳密文件，不建議放進 Project 檔案區，僅在需要核對時單次上傳給 Claude 比對
