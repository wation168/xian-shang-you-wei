# SoftGlow / 線上有位 — 目前狀態總覽（精簡控制台版）

> 這份文件是「單一入口」。每次新對話開始，先讀這份，不用帥哥鴻重講背景。
> **這份文件只放：最新1~2輪摘要、還在進行中/待驗證的事、還沒拍板的事、操作教訓、文件索引。
> 所有「已經完全結案、已部署」的項目一律不留在這裡，全部收在《claude/已完成事項歸檔.md》，
> 這裡最多留一行bullet＋連結。想查某個舊功能是哪輪做的、有沒有上線，去查那份歸檔文件，
> 不用整份讀這份總覽。**
> 更新原則：每次有實質修改/發現，Claude 主動重新輸出這份文件；等一個回合真正告一段落（要收工、或有關鍵結果進來）再一次寫完整，不要每完成一個小任務就重寫一次。
> **待辦類清單裡的項目，只要當下查證或處理過就要立刻同步更新掉，不能只在聊天視窗裡回報完就結束卻沒寫回文件。**
> **這份文件沒有局部patch，每次更新都要整份重寫，所以必須維持精簡**：新工程的完整技術敘事一律寫進獨立主題文件（如`claude/遊戲區多語言架構.md`），已結案的項目搬進《claude/已完成事項歸檔.md》，這裡只留指標。

---

## 最新進度（1~2輪摘要）

> **2026/08/27（第四十五輪）— 修正「線上有位」桌面版拉不到最下面的bug、深度選股門檻改成admin API可動態調整、新增App完整使用說明書(guide.html)三處入口、太陽星座/生肖流年/塔羅牌命理系列部署狀態全面實測確認**：本輪起點是帥哥鴻回報「線上有位電腦版畫面拉不到最下面，tab會擋住」。查證發現根因是深度選股跑馬燈`#deepAnalysisMarquee`（今天有新選股時才出現，佔用37px版面高度）沒有被桌面版三欄版面`.app`的`height:calc(100vh - 56px - 56px)`計算式扣除，導致中欄內容框多撐出37px、恰好被固定在畫面底部的`.bottom-nav`蓋住。**修法走了兩輪**：第一版新增CSS變數`--marquee-h`（由JS即時量測跑馬燈`offsetHeight`寫入，不寫死高度數字），但誤把`margin-top`也加上`var(--marquee-h)`，跟跑馬燈本身`position:static`在文件流裡已經自然佔位的37px重複扣了兩次，反而讓桌面版更往下推、問題沒解決；帥哥鴻二次回報後重新用瀏覽器實測揪出重複計算，修正為只調整`height`/`max-height`扣除跑馬燈高度、`margin-top`維持固定`56px`不變，實測捲到底後內容完整露出、不再被導覽列蓋住，已部署確認生效。**同批完成深度選股門檻可調整化**：`main.py`新增`app_settings`（key-value）資料表、`_get_deep_cfg_overrides()`/`_apply_deep_cfg_overrides()`輔助函式、`GET/POST /admin/deep-scan-config`兩個新端點（沿用既有`X-Admin-Key` header驗證模式），`_run_deep_analysis_job()`每次執行前會先套用DB裡的覆寫值到`finmind_filter.DEEP_CFG`，沒被覆寫的欄位維持`finmind_filter.py`裡的原始預設值——之前"深度選股一次選22檔太多"這類門檻調整，之後透過API呼叫即可，不用再改程式碼、不用重新部署；已用`curl`打正式站`GET /admin/deep-scan-config`（不帶key）確認回傳403（不是404），端點確實已註冊生效。**新增App完整使用說明書**：新檔案`backend/frontend/guide.html`（比照App配色含亮/暗主題，靠讀取共用`localStorage['theme']`跟App視覺同步），涵蓋快速開始／看懂分析結果／K線圖表操作／自選股與關注頁／選股（開盤熱門＋深度選股信心等級）／持股健檢／討論區／到價提醒與通知設定／我的帳號與方案／邀請好友解鎖／免費版vs付費版比較表／常見問題共12個章節，靠`main.py`既有的`@app.get("/{filename}.html")`萬用路由自動服務、不用額外加路由。在`index.html`補上三處入口：桌面版頂部導覽列新增「📖使用說明」連結、「我的」頁面訪客與會員兩種狀態都補上（會員狀態同時補了「💼持股健檢」快捷按鈕）。**過程中意外發現一個既有UX缺口**：手機版底部5個分頁（分析/關注/選股/討論/我的）完全沒有「持股健檢」的入口，只有電腦版三欄頂部分頁籤才看得到，`showPage('portfolio')`只被桌面版按鈕呼叫過——已比照「發現既有bug直接主動修掉」的既定原則，在「我的」頁面補上手機版可用的捷徑一併修復，已實測`showPage('portfolio')`從新按鈕觸發後正常顯示新增持股表單。**部署過程踩到CDN負快取教訓（文件裡早有記載但這輪又踩了一次）**：太陽星座/生肖流年/塔羅牌那批（commit `b5b2b04f`）其實在更早之前就已經commit+push成功，但用WebFetch直接查`/tools/sun-sign-calculator.html`等網址時，太陽星座跟生肖流年計算機顯示語言互換（沒有語言前綴的網址顯示英文、`/en/`反而顯示中文）、塔羅牌兩語言都404，一度誤判為部署失敗；改用帶`?cachebust=`參數的網址重新查證後，六個頁面（中英各3個）全部正確——證實是CDN對這幾個網址先前（修正前）查詢結果的負向快取還沒過期，不是真的沒部署成功。已同步確認首頁英文版entry-grid的「命理風水」卡片正確連到`bazi-calculator.html`。**目前所有本輪異動（scroll bug最終修正版、DEEP_CFG admin API、guide.html+三處入口）都已用瀏覽器/WebFetch實測確認正式站生效，太陽星座/生肖流年/塔羅牌/首頁命理風水卡片也重新確認無誤。**
>
> **2026/08/23（第四十四輪）— 八字計算機剩餘8語言（de/es/fr/id/ja/ko/pt/zh-CN）全部完成上線，10語言全量結案；同步修復9語言首頁「命理風水」入口卡片連結**：延續10-7節已驗證的en/zh-TW版本，採用「內容隔離模板化」策略——把約340KB的檔案精準切成三段（可翻譯head/body/chrome、含LunarJS函式庫＋computeBaziData＋renderResults的巨型`<script>`區塊逐字重用、`BZ_L`標籤JSON），只把8語言各自的翻譯JSON交給8個平行research agent翻譯，再用Python組裝腳本套進固定模板。十神翻譯規則依語言分流（德/西/法/印尼/葡文翻譯英文詞保留漢字；日/韓文用該語言慣用術語；簡體中文繁轉簡）。完整驗證管線＋額外收尾（hreflang補齊10語言、8語言feng-shui-bedroom雙向互連、9語言首頁入口卡片改連bazi-calculator）詳見《Pinterest圖卡網站_15分類規劃_定案版.md》10-10節。
>
> 其餘更早（塔羅牌/太陽星座/生肖流年運勢EN+zh-TW初版、9語言entry-grid初版升級等）收在下方「進行中」清單與連結，不重複展開。

其餘更早的已完成項目（分類整理：遊戲區/樂透彩票/LINE整合/深度選股/工具頁SEO/基礎建設）全部收在《claude/已完成事項歸檔.md》，含完整檔案架構對照表。

---

## ⏳ 進行中 / 待驗證（真正還在等結果的）

- 🔲 **自選股族群功能（電腦版左側常駐清單＋手機版首頁都能自訂分類，關注頁維持原樣不變）已多次隨index.html異動一起commit+push，但本輪沒有專門實測這個功能本身**：下次對話要專門用瀏覽器驗證（1）電腦版左側是否顯示「我的自選股」（2）手機版首頁族群篩選＋分類功能是否正常（3）手機版「關注」頁是否維持原樣（4）電腦版中欄是否正確隱藏了首頁的族群篩選列（5）登入帳號後打開首頁不會出現「今日查詢次數已用完」的提示
- 🔲 **既有bug：手機版「關注」頁的分頁籤（⭐自選股／📈戰績日誌）疑似會被自己的內容渲染覆蓋掉**：這輪沒有處理，下次有空要單獨排時間查證+修正，牽涉`showWatchListWithTabs()`/`_renderWatchInner()`/`showWatchList()`三支函式的渲染目標對不上的問題
- 🔲 **首頁「大盤脈動」（加權指數/上漲下跌家數）**：Design草案畫的展示內容，後端完全沒有市場指數資料源，帥哥鴻如果之後想要要先討論資料來源
- 🔲 **Design畫布六頁草案，個股分析頁/深度選股/討論/我的頁面「之後慢慢調整」**：帥哥鴻明確表示不急，等他之後提出再處理；畫布連結：https://claude.ai/code/artifact/70c0fde9-f30c-426b-9918-7ce2306c6155
- 🔲 **深度選股路徑B「剛突破」機制上線後的實際效果**：回測邏輯已驗證、正式站已部署確認生效，實際選股品質建議觀察一段時間
- 🔲 **深度選股門檻現在已可透過`/admin/deep-scan-config`調整，但實際入選檔數變化要等下一次交易日17:00排程跑完才能驗證**
- 🔲 **韓國樂透改回官方API＋`/patterns/en.html`補canonical，git已push，但尚未重新實測`/patterns/en.html`是否已看到canonical生效**：下次對話用WebFetch（記得帶cachebust參數避免CDN負快取誤判）複查
- 🔲 **遊戲區13款遊戲10語言版本的手機/觸控裝置細節體驗**：已確認可正常遊玩，還沒特別針對手機觸控細節驗證
- 🔲 **遊戲區登入提醒雙層機制上線後的實際效果**
- 🔲 **防守位候選A上線後的實際效果**：回測是歷史資料模擬，上線後建議觀察一段時間
- 🔲 **彩票首頁加厚後的SEO效果**：需等數週GSC新數據
- 🔲 **LINE登入/LINE Bot上線後的實際使用狀況**：尚未有真實使用者走完整流程

## 🔲 還沒開始 / 待你拍板方向

- **紫微斗數計算機**：依《Pinterest圖卡網站_15分類規劃_定案版.md》10-4排定順序，風水命卦、八字（10語言全量）、生肖流年運勢、太陽星座、塔羅牌（皆EN+zh-TW）已動工，下一個里程碑（用`iztro`為底＋5語言字典）
- **命理系列擴充路線圖（紫微斗數之後）**：西方候選（星座＋塔羅）已全數結案，剩餘候選為中式：易經六爻占卜、姓名學（不同源、優先度較低）。下一個要做哪個尚未拍板，完整記錄見《Pinterest圖卡網站_15分類規劃_定案版.md》10-9節
- **生肖流年運勢／太陽星座／塔羅牌剩餘8語言（de/es/fr/id/ja/ko/pt/zh-CN）**：技術架構已就緒（各自沿用`zodiac_core.js`/`sunsign_core.js`/`tarot_core.js`＋`labels`資料物件模式，皆不需曆法函式庫），等排入下一輪
- **命理系統SaaS付費層方向**：技術架構已定案，帥哥鴻確認暫緩，命理系列先以免費工具為主
- **首頁「大盤脈動」**：需要先討論市場指數資料來源（FinMind有沒有大盤指數API、還是要另外找）
- **學測評量系統：整條線已暫停**（版權疑慮未解），詳見《學測評量系統_目前狀態總覽.md》；相關檔案（`scripts/`底下考題爬取腳本、`quiz_backend.zip`、`學測評量/`資料夾等）目前刻意留在git untracked狀態不commit，等專案重啟再處理
- **保健品購物網站、AI 被動收入教學文件**：下階段大專案，尚未開始討論細節

---

## ⚠️ 給我自己（Claude）的提醒（高頻使用的規則，保留在主文件；完整歷史版本見歸檔文件）

- **這份總覽沒有局部patch，每次更新都整份重寫，必須保持精簡**：完整技術敘事寫獨立主題文件，結案項目搬進歸檔文件，這裡只留指標；舊教訓被新教訓取代時直接刪除，不要無限累加
- **修正「A元素高度會影響B元素版面」這類CSS bug時，要先搞清楚A元素是`position:static`（在文件流裡自然佔位，會自動推擠後面的元素）還是`fixed`/`absolute`（脫離文件流，不會自動推擠）——只有非static定位的元素才需要額外手動補償margin/padding，static元素的佔位效果已經算在正常文件流裡，重複補償會算兩次**（這輪`--marquee-h`踩過：`margin-top`不該再加一次跑馬燈高度，只有`height`需要扣）；改完CSS calc公式，一定要重新用瀏覽器JS讀`getBoundingClientRect()`實際數字驗證頭尾兩端是否真的對齊，不能只看公式邏輯覺得「應該對」
- **查證「檔案是否已部署」時，如果WebFetch/瀏覽器查到的結果跟git log顯示已push的內容矛盾（尤其是先前才剛查過同一個網址、或該網址先前曾經404過），優先懷疑CDN負向快取而不是急著下「部署失敗」的結論**：用帶`?cachebust=時間戳`的網址重新查一次，如果內容變正確就證實是快取問題；這個教訓文件裡記過但這輪還是先誤判了一次，要更早想到
- **git commit分批給指令時，如果使用者回報跟預期不符（例如某批內容明明該有變化但查證起來沒有），要主動請使用者貼`git log --oneline`＋`git status`重新核對，不要預設使用者一定把每一批指令都跑過**——這輪「命理系列工具」那個commit一開始沒在使用者第一次貼的log裡出現（後來發現是使用者只貼了部分結果，不是真的沒commit），先弄清楚是「真的沒commit」還是「只是查證方式/複製範圍不夠完整」再下結論
- **改動一段既有程式碼時，如果發現旁邊有明顯不對勁但跟這次需求無關的既有bug，要記錄下來、評估風險後再決定當下修不修**；帥哥鴻已明確表示希望這類發現直接主動修掉，不用等被問到（這輪：手機版完全沒有「持股健檢」入口，已主動補上）
- **`device_stage_files`會用裝置端當下內容覆寫本地已編輯檔案**——重新stage同一批檔案前，一定要先確認本地是否有還沒寫回裝置的編輯，改用`device_list_dir`核對mtime較安全，不會有覆寫風險
- **多語言頁面上「同一個命理系列總覽卡片」跟「單一工具的獨立入口卡片」是兩種不同東西，改連結時要分清楚只改前者**
- **驗證新工具「算得對不對」不夠，還要專門驗證「邊界條件/非法輸入有沒有處理」**，完整案例見《新工具規劃守則.md》第九節
- **新工具做完頁面本身不算結案，一定要確認`/tools/index.html`等總覽頁有沒有收錄、同系列既有工具有沒有互相回連**，完整檢查清單見《新工具規劃守則.md》第七節
- **命理計算機這類「算法正確性是核心賣點」的工具，優先查證是否有MIT等允許商用授權的成熟開源函式庫，不要自己憑統計傾向推導傳統命理公式**
- **同一套曆法函式庫可能提供不只一種「年份分界」邏輯，不同命理系統慣用的分界不一定相同**——動工前要先查證清楚
- **內容量大的翻譯/文案類素材可以委託research agent先產出初稿，但一定要自己驗證資料結構完整性＋抽樣讀內容品質**；大型檔案翻譯時只把「結構化英文JSON」交給agent，絕對不要讓agent看到原始HTML/JS
- **多語言工具頁在還沒把全部語言版本做完時，hreflang只列「當下真的存在」的語言＋x-default；等全部語言都做完後記得回頭補齊最早上線那幾個檔案**
- **同一個工具的跨語言連結只能連到「該語言確實存在」的頁面**——動工前先確認目標語言目錄下有沒有那個檔案
- **帥哥鴻確認UI放置位置時，即使問過、答案裡若沒有明確講死「放在哪一頁」，還是可能理解錯**——UI放置位置這種空間性模糊指示，寧可多問一句
- **同一份手機版/電腦版共用DOM，靠CSS media query切版面時，新增手機專用UI要記得檢查它會不會也在電腦版顯示出來**
- **任何會在使用者「還沒主動操作」前就自動打API的新功能，動手前一定要先查後端那支API有沒有查詢額度限制**
- **改動大型前端檔案後，一定要先用`node --check`驗證inline script語法、用diff比對確認只有預期的變更**，才寫回使用者電腦
- **文件裡「已經部署過」的說法，動手給部署指令前一定要對正式站做實際查證**
- **「code已經git push上去」不等於「正式站已經部署那個版本」**
- **「選出的數量太多」類反饋，先查清楚是不是真的卡在某個寫死的數字上限，還是根本沒有分數/品質門檻**
- **任何技術發現/GSC異常，先查《claude/已完成事項歸檔.md》有沒有已經處理過的紀錄再回報為新問題**
- **部署驗證不能只看HTTP狀態碼，一定要實際打開瀏覽器動手操作**
- **改main.py之前一定要重新從帥哥鴻電腦即時抓最新版本**
- **這個雲端環境沒有能直接對帥哥鴻電腦執行git指令的工具**
- **git push被拒絕（rejected/fetch first）代表遠端有本機沒有的commit**
- **使用者說「你自行處理」是針對Claude剛才明確提出的具體選項的一次性授權**
- 使用者明確要求「都打中文」時，全程改用中文回應
- 使用者明確要求CMD格式的指令，不要用bash風格
- 敏感資訊要主動提醒使用者不要貼進對話，引導直接填入平台環境變數
- 正式上線前用claude-in-chrome直接連production screenshot/get_page_text驗證
- 部署要「做一起」——邏輯上同一批的檔案合併成單一`git add`+`commit`+`push`；只add要部署的目錄/檔案，絕不用`git add .`
- 使用者用籠統說法時先用AskUserQuestion確認範圍；操作指令一次只給一步
- main.py不是可import的模組
- 雲端沙盒連不到FinMind/TWSE/lottolyzer等外部市場資料/第三方API
- 任何「已修復/已存在」的宣稱一律要實測驗證；帥哥鴻說「已經修好了」時直接採信、查證當下狀態即可
- 首頁是`homepage.html`不是`index.html`；「線上有位」股票分析App實際路徑是`/stock/`；語言版首頁路徑是`/home/{locale}/`不是`/{locale}/`
- 主動建議合適的模型——簡單任務Haiku、一般開發Sonnet、複雜工程才Opus/Claude Code
- 帥哥鴻對重複犯錯的容忍度很低——同類流程務必從源頭避免

---

## 📁 文件索引（找東西時查這裡，不用整份讀總覽）

| 文件 | 內容 |
|---|---|
| `claude/已完成事項歸檔.md` | **所有已結案、已部署項目的完整清單**（依遊戲區/樂透/LINE/深度選股/工具頁/基礎建設分類）＋完整檔案架構對照表＋完整歷史教訓版本 |
| `claude/遊戲區多語言架構.md` | 遊戲區13款×10語言完整架構、建置工具鏈、部署事故記錄與教訓 |
| `claude/歷史紀錄_第三十輪遊戲開發詳情.md` | 12+1款遊戲當初的完整任務拆解與技術決策細節 |
| `學測評量系統_目前狀態總覽.md` | 獨立專案，整條線已暫停（版權疑慮未解） |
| `新工具規劃守則.md` | 新工具/新頁面開發前的檢查清單（貨幣在地化、時效性內容、生成腳本安全、驗證方式、新工具入口/可發現性檢查、防範AI寫死程式碼、八字計算機事後稽核抓到的寫死年份/日期驗證真實案例） |
| `Pinterest圖卡網站_15分類規劃_定案版.md` | Pinterest 圖卡第二條線規劃＋命理系統市場定位、技術架構、實作進度——風水命卦10語言（10-6）、八字10語言全量（10-7＋10-10）已結案，生肖流年運勢、太陽星座、塔羅牌EN+zh-TW均已完成上線並於2026/08/27重新實測確認生效（西方候選全數結案，見10-9），剩餘3個工具的語言＋紫微斗數本身待續 |
| `main_py_結構盤點表_最終合併版.md` | ⚠️ 已過期作廢 |
