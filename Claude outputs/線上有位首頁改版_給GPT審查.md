# 線上有位首頁改版 — 草稿 + 技術現況回報（給GPT參考／審查）

> 這份是照三方協作新流程補的「Claude技術現況回報」，讓GPT審查/後續設計時有真實地基，不是憑空看草稿。

## 一、技術現況盤點（草稿之前先查證過的事實）

- 帥哥鴻你附的參考圖**不是**線上有位現在的樣子。實際首頁是 `index.html` 裡的 `#homeDash` 區塊，2026/08/22新增、09/12又被改過一輪（commit: `Wave1 Data-1 IA-Home-1: member credits, market APIs, V5 flow home`），目前長得是「一張張白卡片」的版本。
- 現有首頁的資訊動線其實已經跟GPT brief的六大區塊骨架很接近（市場→產業→今天看什麼→個股分析→我的投資→深度分析），只是視覺很陽春，**不是要整個重新發明，是重新設計外觀+互動**。
- 逐區真實資料來源／函式：
  | 區塊 | 現況 |
  |---|---|
  | 今天台股怎麼了（指數） | 真實API `/api/market/overview`，已有加權/櫃買指數、漲跌%、來源、更新時間 |
  | 市場氣氛／漲跌家數（上漲712/下跌490/平盤120這種全市場統計） | **系統沒有，真的是【建議新增】**，需要後端新寫一支統計API |
  | 錢往哪裡去（產業強弱） | 真實API `/api/market/industries`，已回傳gainers/losers |
  | 今天看什麼（市場熱門/開盤精選/深度選股） | 真實函式 `_homeGo('quotes'/'picks'/'deepanalysis')`，會跳轉到既有頁面，不是假按鈕 |
  | 想分析一檔股票 | 真實函式 `_homeFocusSearch()` + `historyList`（既有最近查詢紀錄）|
  | 我的投資（自選+持股健檢） | 真實資料：watchList、`/batch_analyze`批次報價、`/portfolio/analysis`。「你的持股中有N檔值得注意」是可以從既有 `_suggest()` 邏輯（依KD/MACD/均線算出的持股建議）彙總算出來的，**不是新演算法**，只是新的呈現方式 |
  | 深度分析／會員升級 | 真實邏輯 `_homeGo('deep')`、`showUpgradeModal()`、`_hasPremium()`，付費判斷都是現成的 |
- 網站已有明暗主題系統（CSS變數 `--bg/--up/--dn/--green` 等，紅漲綠跌台股慣例），新設計會沿用不會另外造一套。
- 桌面版6個分頁籤／手機版底部導覽5個分頁籤都已存在。

## 二、草稿

Artifact連結（帥哥鴻電腦可直接打開，互動式，可縮小視窗看手機版、可切換深色、可點「顯示技術對照」看每區塊對應的真實函式）：
https://claude.ai/code/artifact/17d84fdd-f2af-4d26-92f2-b75d623a3712

附上桌面版/手機版截圖（desktop.png / mobile.png）方便直接貼給GPT看。

### 六區設計重點
1. **今天台股怎麼了**：大主視覺卡，指數內嵌走勢sparkline + 市場氣氛漲跌家數橫條（新增部分）
2. **錢往哪裡去**：5個產業卡片，強弱用色條可視化，可點擊進產業頁
3. **今天看什麼**：3張差異化配色卡片（紅/藍/綠），各自標示鎖定狀態（依會員權限顯示／付費會員完整開放）
4. **想分析一檔股票**：大搜尋框+開始分析按鈕+最近查詢chip，右側示意K棒圖
5. **我的投資**：左邊自選清單+持股警示彙總，右邊持股健檢環狀圖
6. **深度分析／會員價值**：金色系跟其他區塊區隔，模糊遮罩預覽+解鎖CTA，走「先展示訊號、再引導升級」邏輯

## 三、想請GPT確認的點

1. 市場氣氛/漲跌家數這個新功能的呈現方式（橫條+文字）OK嗎，還是要別的視覺表現？
2. 三張「今天看什麼」卡片的紅/藍/綠配色跟深度分析區的金色，整體色彩系統GPT看過去OK嗎？
3. 有沒有哪個區塊的資訊層級/排序需要調整？
4. 這版可以進到「Claude直接動手改真正的index.html」這一步了嗎？
